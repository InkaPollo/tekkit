local flagFile = "/.BOOT_IN_PROGRESS"

if fs.exists(flagFile) then
    return
end

-- Create flag immediately
local f = fs.open(flagFile, "w")
f.write("1")
f.close()

sleep(1)
term.clear()
term.setCursorPos(1, 1)
shell.run("startup3", "4")

-- Clean up after done
fs.delete(flagFile)
