-- Redirect to left monitor
local monitor = peripheral.wrap("left")
if not monitor then
    error("Left monitor not found!")
end
term.redirect(monitor)

-- Set text scale to 1
monitor.setTextScale(1)

-- Run shell on the left monitor
shell.run("shell")
